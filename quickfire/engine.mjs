// Pure game rules. No DOM, timers, audio or network dependencies.
export const SIZE = 15;
export const ALPHABET = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
const WEIGHTED = 'AAAAAAAABCDEEEEEEEEEEEFGHHIIIIIIJKLMNNOOOOOPRRRRSSSSSTTTTTTTUUUVWY';
const COMMON = `ABLE ACORN ACTOR ACUTE ADAPT ADMIT ADOPT ADULT AFTER AGAIN AGENT AGILE AGREE AHEAD ALARM ALBUM ALERT ALIEN ALIVE ALLOW ALONE ALONG AMBER ANGEL ANGRY ANIMAL APPLE APRON ARGUE ARISE ARMOUR ARROW ASH ATLAS AVOID AWAKE AWARD AWARE BACON BADGE BAKER BALLET BANANA BASIC BASIL BATCH BEACH BEADS BEAM BEANS BEARD BEAST BEAUTY BEGIN BELOW BENCH BERRY BICYCLE BIRCH BIRD BIRTH BLACK BLADE BLAME BLANK BLAST BLAZE BLEND BLESS BLIND BLOCK BLOOM BLOSSOM BLUE BLUNT BOARD BOAST BOAT BOLD BONE BONUS BOOK BOOTS BOUND BOWL BOX BRAIN BRANCH BRASS BRAVE BREAD BREAK BRICK BRIDE BRIEF BRIGHT BRING BROAD BROOK BROOM BROWN BRUSH BUBBLE BUDGET BUILD BUNCH BUNNY BURST BUTTER CABIN CABLE CAKE CALM CAMEL CANDLE CANDY CANOE CAPE CARE CARGO CARRY CARVE CAST CAT CATCH CAUSE CAVE CEDAR CHAIN CHAIR CHALK CHARM CHASE CHEAP CHEEK CHEER CHERRY CHESS CHEST CHICK CHILD CHILL CHIME CHIN CHIPS CHOIR CHOOSE CHORD CIDER CIRCLE CITY CLAIM CLAP CLASS CLEAN CLEAR CLERK CLIFF CLIMB CLOCK CLOSE CLOUD CLOWN COACH COAST COCOA COIN COLD COLOUR COMET CORAL CORN COUCH COUNT COURT COVER CRAFT CRANE CRASH CRAWL CREAM CREEK CRISP CROSS CROWN CRUMB CRUSH CURL CURVE CYCLE DAILY DAIRY DAISY DANCE DART DAWN DEAL DEAR DEEP DEER DELAY DELIGHT DENIM DESK DIAL DIARY DICE DIRT DISCO DISH DIVE DOG DOLPHIN DOOR DOUBT DOUGH DOVE DRAFT DRAIN DRAMA DRAW DREAM DRESS DRIFT DRINK DRIVE DROP DRUM DRY DUCK DUST DWARF EAGER EAGLE EAR EARLY EARTH EASE EAST EASY ECHO EDGE ELBOW ELDER EMPTY ENJOY ENTER EQUAL ERROR EVENT EXACT EXTRA FACE FACT FAIR FAIRY FAITH FALL FALSE FAMILY FAMOUS FANCY FARM FAST FAVOUR FEAST FEATHER FEED FEEL FENCE FERRY FETCH FIELD FIERY FIGHT FILM FINAL FINCH FIND FINE FIRE FISH FIST FIVE FIX FIZZY FLAG FLAKE FLAME FLASH FLEET FLESH FLIGHT FLOAT FLOCK FLOOD FLOOR FLOUR FLOWER FLUFF FLUTE FLY FOCUS FOLD FOOD FOOT FORCE FORK FORTH FORTY FOUND FOX FRAME FRESH FROG FRONT FROST FRUIT FUDGE FUNNY FUZZY GAME GARDEN GATE GAZE GENTLE GHOST GIANT GIFT GIRAFFE GIRL GIVE GLAD GLASS GLEAM GLIDE GLOBE GLOOM GLOSS GLOVE GLOW GOAL GOAT GOLD GOOSE GRACE GRADE GRAIN GRAND GRAPE GRASS GRAVE GREAT GREEN GRILL GRIN GROW GUARD GUESS GUEST GUIDE GULL GUST HABIT HAIR HALF HALL HAMMER HAND HAPPY HARE HARP HASTE HATCH HAT HAZEL HEAD HEAR HEART HEAT HEAVY HEDGE HEEL HELLO HELP HEN HERB HERO HIGH HILL HINT HIVE HOLD HOLE HONEY HOOK HOPE HORSE HOTEL HOUSE HUMAN HUMOUR HURRY HUSKY ICE IDEAL IMAGE INCH INDEX INK INNER INPUT IRON ISLAND IVORY JADE JAM JAR JAW JAZZ JELLY JET JEWEL JOIN JOKE JOLLY JOLTS JOY JUDGE JUICE JUMP JUNGLE KEEN KEEP KETTLE KEY KICK KIND KING KITE KITTEN KNEE KNIFE KNIT KNOCK KNOT KNOWN LABEL LACE LADDER LADY LAKE LAMB LAMP LAND LARGE LASER LATE LAUGH LAVA LAWN LAYER LEAF LEAN LEAP LEARN LEAST LEAVE LEMON LEND LENGTH LEVEL LIGHT LILAC LIME LIMIT LINE LION LIP LIST LITTLE LIVE LIZARD LOAD LOAF LOAN LOBBY LOCAL LOCK LODGE LOGIC LONE LONG LOOK LOOP LOOSE LOTUS LOUD LOVE LOWER LUCK LUCKY LUNAR LUNCH LYRIC MAGIC MAJOR MAKE MANGO MANOR MAPLE MARCH MARSH MASK MATCH MAYBE MEAL MEAN MEDAL MELON MELT MERCY MERGE MERRY METAL MIDDLE MIGHT MILD MILK MILL MIND MINT MIRROR MIX MODEL MONEY MONTH MOOD MOON MOOSE MOSS MOTH MOTOR MOUSE MOUTH MOVE MOVIE MUD MUSIC NAIL NAME NARROW NATURE NAVY NEAR NEAT NEED NERVE NEST NEVER NEW NEWT NEXT NICE NIGHT NOBLE NOISE NORTH NOSE NOTE NURSE OAK OCEAN OFFER OFTEN OLIVE ONCE ONION OPAL OPEN OPERA ORANGE ORBIT ORDER ORGAN OTTER OVEN OWL OWNER OYSTER PACE PACK PAGE PAINT PAIR PALACE PALM PANDA PANEL PAPER PARK PARTY PAST PASTA PATCH PATH PAUSE PEACE PEACH PEAR PEARL PEDAL PEEP PENNY PEPPER PERCH PET PETAL PHONE PHOTO PIANO PICK PICNIC PICTURE PIECE PIG PILOT PINCH PINE PINK PINT PIPE PIZZA PLACE PLAIN PLANE PLANT PLATE PLAY PLEAD PLOT PLUCK PLUM PLUSH POCKET POEM POINT POLAR POLISH POND POOL POPPY PORCH POST POTATO POUND POWER PRESS PRICE PRIDE PRIME PRINT PRIZE PROUD PROVE PULSE PUPPY PURPLE QUEEN QUEST QUICK QUIET QUIZ QUOTE RABBIT RACE RADIO RAIN RAISE RALLY RANCH RANGE RAPID RARE RAVEN REACH READ READY REAL REASON RED REED REEF RELAX REPLY REST RHYME RICE RICH RIDE RIGHT RING RISE RIVER ROAD ROAST ROBIN ROBOT ROCK ROLL ROOF ROOM ROOT ROPE ROSE ROUGH ROUND ROUTE ROYAL RUBY RUG RULE RUSH RUST SAD SAFE SAGE SAIL SALAD SALT SAND SANDY SAUCE SAVE SCALE SCARE SCARF SCENE SCENT SCHOOL SCORE SCOUT SEA SEAL SEARCH SEAT SEED SEEK SEEM SENSE SERVE SEVEN SHADE SHAKE SHAPE SHARE SHARK SHARP SHEEP SHEET SHELF SHELL SHINE SHIP SHIRT SHOCK SHOE SHOP SHORE SHORT SHOUT SHOW SHOWER SHRUB SHY SIDE SIGHT SIGN SILK SILLY SILVER SING SINK SISTER SIX SIZE SKATE SKILL SKIN SKIP SKIRT SKY SLATE SLEEP SLICE SLIDE SLOPE SLOW SMALL SMART SMELL SMILE SMOKE SNAIL SNAKE SNAP SNEAK SNOW SOAP SOCK SOFA SOFT SOIL SOLAR SOLID SOLVE SONG SOON SOUP SOUTH SPACE SPADE SPARK SPEAK SPEED SPELL SPEND SPICE SPIKE SPILL SPIN SPINE SPOON SPORT SPRAY SPRING SQUARE STACK STAGE STAIN STAIR STAMP STAND STAR START STATE STAY STEAM STEEL STEM STEP STICK STILL STONE STOOL STOP STORE STORM STORY STOVE STRAW STRIPE STUDY STYLE SUGAR SUIT SUMMER SUN SUNNY SUPER SWAN SWEET SWIM SWING SWIRL TABLE TAIL TAKE TALE TALK TALL TANK TAPE TASTE TAXI TEACH TEAM TEAR TEDDY TEETH TEMPO TENT THANK THEME THICK THIEF THIN THING THINK THIRD THORN THREE THROW THUMB TICK TIDE TIGER TILE TIME TINY TIRED TOAST TODAY TOFFEE TOKEN TONE TOOTH TOPIC TORCH TOTAL TOUCH TOUGH TOWER TOWN TRACE TRACK TRADE TRAIL TRAIN TREAT TREE TREND TRIAL TRICK TRIP TROLL TROUT TRUCK TRULY TRUST TRUTH TUNE TUNNEL TURN TWICE TWIG TWIRL TWIST UNCLE UNDER UNION UNITE UNITY UNTIL UPPER URBAN USUAL VALID VALUE VANILLA VASE VEIL VELVET VENUS VERSE VIDEO VIEW VILLA VINYL VIOLIN VISIT VIVID VOICE VOTE WAFFLE WAGON WAIST WAIT WAKE WALK WALL WALNUT WARM WARN WASH WASTE WATCH WATER WAVE WAX WEAR WEAVE WEDGE WEED WEEK WEIGH WEIRD WELL WEST WHALE WHEAT WHEEL WHERE WHICH WHILE WHITE WHOLE WIDE WILD WILL WIND WING WINNER WINTER WIRE WISE WISH WITCH WOLF WOMAN WONDER WOOD WOOL WORD WORLD WORM WORRY WORTH WOULD WOUND WRAP WREN WRIST WRITE WRONG YACHT YARD YARN YEAR YEAST YELLOW YES YET YIELD YOUNG YOUTH ZEBRA ZEST ZINC ZONE ZOOM`.split(/\s+/);

export function seededRandom(seed) { let n=seed>>>0; return () => { n+=0x6D2B79F5; let t=n; t=Math.imul(t^(t>>>15),t|1); t^=t+Math.imul(t^(t>>>7),t|61); return ((t^(t>>>14))>>>0)/4294967296; }; }
export function shuffle(items,rng=Math.random) { const a=[...items]; for(let i=a.length-1;i>0;i--){const j=Math.floor(rng()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a; }
const pick=(items,rng)=>items[Math.floor(rng()*items.length)];
const indices=(row,col,length,dir)=>Array.from({length},(_,i)=>(row+(dir==='down'?i:0))*SIZE+col+(dir==='across'?i:0));

// Filler stays outside the crossword. Only complete, maximal occupied runs
// count, and every run through a placed letter must be valid.
export function wordRuns(occupied,index) {
  const row=Math.floor(index/SIZE),col=index%SIZE,runs=[];
  for(const direction of ['across','down']) {
    const line=Array.from({length:SIZE},(_,i)=>direction==='across'?row*SIZE+i:i*SIZE+col);
    const pivot=direction==='across'?col:row;let lo=pivot,hi=pivot;
    while(lo>0&&occupied[line[lo-1]])lo--;
    while(hi<SIZE-1&&occupied[line[hi+1]])hi++;
    if(hi>lo)runs.push({direction,cells:line.slice(lo,hi+1)});
  }
  return runs;
}
export function findWords(board,index,letter,dictionary,occupied=board.map(l=>l!==undefined)) {
  if(board[index]!==null||!occupied[index]||!/^[A-Z]$/.test(letter))return [];
  const matches=[];
  for(const {direction,cells} of wordRuns(occupied,index)) {
    if(cells.length<3||cells.some(id=>id!==index&&board[id]===null))return [];
    const word=cells.map(id=>id===index?letter:board[id]).join('');
    if(!dictionary.has(word))return [];
    matches.push({word,direction,cells,key:`${direction}:${cells[0]}:${word}`});
  }
  return matches;
}
export function solutionsFor(board,index,dictionary,occupied) {
  const result=new Map();if(board[index]!==null)return result;
  for(const letter of ALPHABET){const words=findWords(board,index,letter,dictionary,occupied);if(words.length)result.set(letter,words);}
  return result;
}
export function allSolutions(board,dictionary,occupied) {
  const result=new Map();
  for(let i=0;i<board.length;i++)if(board[i]===null)result.set(i,solutionsFor(board,i,dictionary,occupied));
  return result;
}
export const GAP_COUNT=15;
export const RACK_SIZE=16;
export function availableMoves(state) {
  const rack=new Set(state.rack.filter((_,slot)=>!state.rackUsed[slot])),moves=[];
  for(const [index,options] of state.options)for(const [letter,words] of options)if(rack.has(letter))moves.push({index,letter,words});
  return moves;
}
export function canAnswer(state,index=state.active) {return state.rack.some((letter,slot)=>!state.rackUsed[slot]&&state.options.get(index)?.has(letter));}
export function orderedWords(words) {return [...words].sort((a,b)=>a.word.length-b.word.length||a.word.localeCompare(b.word)||a.key.localeCompare(b.key));}
export function rewardFor(misses,words,automatic=false) {
  if(automatic)return {colour:'red',points:0};
  if(words.length>1)return {colour:'gold',points:5};
  return misses===0?{colour:'green',points:2}:misses===1?{colour:'amber',points:1}:{colour:'red',points:0};
}
// Fifteen gaps have fourteen transitions, so interpolate to reach 15 exactly.
export function timeAllowance(ordinal) {return 30-Math.round((Math.min(15,Math.max(1,ordinal))-1)*15/14);}
// Only overall active thinking time is used. One bonus point per 15 seconds
// saved against the board budget, capped by manually completed gaps.
export function speedBonus(elapsedMs,manualSolved,budgetSeconds=337) {
  return Math.max(0,Math.min(manualSolved,Math.floor((budgetSeconds-Math.max(0,elapsedMs)/1000)/15)));
}

// Match each unresolved gap to a different unused rack slot. A complete
// matching is a constructive solution: every word is already valid independently
// of other gaps, so filling another matched gap cannot destroy it.
export function assignRack(board,rack,rackUsed,dictionary,options=allSolutions(board,dictionary),preferred=new Map()) {
  const slots=rack.map((_,i)=>i).filter(i=>!rackUsed[i]);
  const gaps=[...options.keys()].sort((a,b)=>options.get(a).size-options.get(b).size);
  const owners=new Map();
  function assign(id,visited){
    const candidates=slots.filter(slot=>options.get(id).has(rack[slot])).sort((a,b)=>(rack[a]===preferred.get(id)?-1:0)-(rack[b]===preferred.get(id)?-1:0));
    for(const slot of candidates){
      if(visited.has(slot))continue;visited.add(slot);
      if(!owners.has(slot)||assign(owners.get(slot),visited)){owners.set(slot,id);return true;}
    }
    return false;
  }
  for(const id of gaps)if(!assign(id,new Set()))return null;
  return new Map([...owners].map(([slot,id])=>[id,slot]));
}

export const SHAPES=['square','circle','plus','ring','A','B','C','D','E','H','O','T'];
export const THEMES=['violet','lagoon','rose','citrus','ocean','berry'];
export function shapeMask(shape) {
  if(!SHAPES.includes(shape))throw new Error('Unknown board shape.');
  return Array.from({length:SIZE*SIZE},(_,id)=>{
    const r=Math.floor(id/SIZE),c=id%SIZE,edge=c<5||c>=10;
    switch(shape){
      case 'circle':return (r-7)**2+(c-7)**2<=58;
      case 'plus':return !(r<4||r>=11)||!(c<4||c>=11);
      case 'ring':case 'O':return !(r>=5&&r<10&&c>=5&&c<10);
      case 'A':return (r>=3||c>=3&&c<12)&&(edge||r<5||r>=7&&r<10);
      case 'B':return c<5||((r>=2&&r<6||r>=9&&r<13)&&c>=10)||((r<4||r>=6&&r<9||r>=11)&&c<13);
      case 'C':return c<5||r<5||r>=10;
      case 'D':return c<5||((r<5||r>=10)&&c<12)||(r>=3&&r<12&&c>=10);
      case 'E':return c<5||r<4||r>=6&&r<9||r>=11;
      case 'H':return edge||r>=5&&r<10;
      case 'T':return r<5||c>=4&&c<11;
      default:return true;
    }
  });
}
const neighbours=id=>{const r=Math.floor(id/SIZE),c=id%SIZE;return [r>0?id-SIZE:-1,r<SIZE-1?id+SIZE:-1,c>0?id-1:-1,c<SIZE-1?id+1:-1].filter(x=>x>=0);};
const geometryCache=new Map();
function geometry(shape,rng) {
  const mask=shapeMask(shape);
  if(!geometryCache.has(shape)) {
    const candidates=[];
    for(const length of [3,4,5,6,7])for(const direction of ['across','down'])for(let r=0;r<SIZE-(direction==='down'?length-1:0);r++)for(let c=0;c<SIZE-(direction==='across'?length-1:0);c++){
      const cells=indices(r,c,length,direction);if(cells.every(id=>mask[id]))candidates.push({cells,direction,buffer:[...new Set(cells.flatMap(neighbours).concat(cells))]});
    }
    geometryCache.set(shape,candidates);
  }
  const candidates=geometryCache.get(shape),crosses=candidates.filter(p=>p.cells.length===5&&p.direction==='across');
  for(let attempt=0;attempt<100;attempt++){
    const h=pick(crosses,rng),shared=h.cells[2],r=Math.floor(shared/SIZE),c=shared%SIZE;
    if(r<2||r>SIZE-3)continue;
    const v={cells:indices(r-2,c,5,'down'),direction:'down'};
    if(!v.cells.every(id=>mask[id]))continue;
    const occupied=new Set([...h.cells,...v.cells]),slots=[h,v];
    let available=candidates.filter(p=>!p.buffer.some(id=>occupied.has(id)));
    while(slots.length<16&&available.length){
      // Prefer a longer early word, then pack shorter words against boundaries.
      const target=slots.length<5?5:slots.length<10?4:3;
      let best=null,bestScore=-Infinity;
      for(const p of available){
        const boundary=p.buffer.filter(id=>!mask[id]||neighbours(id).some(n=>occupied.has(n))).length;
        const score=-Math.abs(p.cells.length-target)*1.8+boundary*.22+rng()*3;
        if(score>bestScore){best=p;bestScore=score;}
      }
      slots.push(best);best.cells.forEach(id=>occupied.add(id));
      available=available.filter(p=>!p.buffer.some(id=>occupied.has(id)));
    }
    if(slots.length===16)return {mask,occupied:[...Array(SIZE*SIZE)].map((_,id)=>occupied.has(id)),slots,shared};
  }
  throw new Error(`Could not fit fifteen gaps in ${shape}.`);
}
function generateLayout(dictionary,rng,shape) {
  const {mask,occupied,slots,shared}=geometry(shape,rng),board=Array(SIZE*SIZE).fill(undefined),planted=[],answers=new Map();
  const pool=COMMON.filter(w=>dictionary.has(w));
  const fives=shuffle(pool.filter(w=>w.length===5),rng);
  const h=pick(fives.filter(w=>fives.some(other=>other!==w&&other[2]===w[2])),rng),v=pick(fives.filter(w=>w!==h&&w[2]===h[2]),rng);
  if(!v)throw new Error('Dictionary lacks a crossing word pair.');
  const chosen=new Set([h,v]);
  for(let n=0;n<slots.length;n++){
    const {cells,direction}=slots[n];
    const word=n===0?h:n===1?v:pick(pool.filter(w=>w.length===cells.length&&!chosen.has(w)),rng);
    if(!word)throw new Error('Dictionary lacks enough familiar words.');
    chosen.add(word);cells.forEach((id,i)=>board[id]=word[i]);
    const gap=n<2?shared:pick(cells,rng);
    answers.set(gap,word[cells.indexOf(gap)]);planted.push({word,cells,direction,gaps:[gap],type:n<2?'double':'normal'});
  }
  for(let i=0;i<board.length;i++)if(mask[i]&&!occupied[i])board[i]=pick(WEIGHTED,rng);
  answers.forEach((_,id)=>board[id]=null);
  return {board,planted,answers,shared,mask,occupied,shape};
}
export function createRound({dictionary,rng=Math.random,score=0,totalWords=0,round=1,shape,previousShape,previousTheme}={}) {
  shape??=pick(SHAPES.filter(s=>s!==previousShape),rng);
  const layout=generateLayout(dictionary,rng,shape),options=allSolutions(layout.board,dictionary,layout.occupied),order=shuffle([...layout.answers.keys()],rng);
  const rack=shuffle([...layout.answers.values(),pick(WEIGHTED,rng)],rng);
  const state={...layout,theme:pick(THEMES.filter(t=>t!==previousTheme),rng),options,rack,rackUsed:Array(RACK_SIZE).fill(null),order,active:order[0],gaps:Object.fromEntries(order.map((id,i)=>[id,{misses:0,shownMisses:0,visits:0,reward:null,ordinal:i+1,allowance:timeAllowance(i+1)}])),found:[],score,totalWords,round,roundPoints:0,roundWords:0,filled:0,manualSolved:0,automaticSolved:0,ended:false,bonusAwarded:false,hintsLeft:3,passesLeft:3,hintSlots:[],hintGap:null};
  state.gaps[state.active].visits=1;
  return state;
}
function nextGap(state,previous) {
  state.hintSlots=[];state.hintGap=null;
  const at=state.order.indexOf(previous);
  for(let offset=1;offset<=state.order.length;offset++){
    const id=state.order[(at+offset)%state.order.length];
    if(state.board[id]===null){state.active=id;state.gaps[id].visits++;state.gaps[id].shownMisses=state.gaps[id].misses;return;}
  }
  state.active=null;
}
function fill(state,index,slot,words,automatic,dictionary,reason) {
  const gap=state.gaps[index],letter=state.rack[slot],reward=rewardFor(gap.misses,words,automatic);
  state.board[index]=letter;gap.reward=reward.colour;state.rackUsed[slot]=automatic?'automatic':'used';
  state.found.push(...words.map(w=>({...w,automatic})));state.score+=reward.points;state.roundPoints+=reward.points;state.totalWords+=words.length;state.roundWords+=words.length;state.filled++;
  if(automatic)state.automaticSolved++;else state.manualSolved++;
  state.options=allSolutions(state.board,dictionary,state.occupied);state.ended=state.options.size===0;nextGap(state,index);
  return {kind:automatic?'automatic':'correct',index,letter,slot,words:orderedWords(words),...reward,ended:state.ended,reason};
}
// Resolve all mutations here so input, keyboard and timer share the same limits.
export function act(state,action,dictionary,rng=Math.random) {
  if(state.ended||state.active===null)return {kind:'ignored'};
  const index=state.active,gap=state.gaps[index];
  if(!['hint','pass','timeout','letter'].includes(action.type))return {kind:'ignored'};
  if(action.type==='hint'){
    if(state.hintsLeft<=0||state.hintGap===index)return {kind:'ignored'};
    const assignment=assignRack(state.board,state.rack,state.rackUsed,dictionary,state.options,state.answers),answer=assignment?.get(index);
    if(answer===undefined)return {kind:'ignored'};
    const unused=state.rack.map((_,slot)=>slot).filter(slot=>!state.rackUsed[slot]);
    const count=Math.max(1,Math.round(unused.length*.3));
    state.hintSlots=shuffle([answer,...shuffle(unused.filter(slot=>slot!==answer),rng).slice(0,count-1)],rng);
    state.hintsLeft--;state.hintGap=index;
    return {kind:'hint',index,slots:[...state.hintSlots]};
  }
  if(action.type==='pass'&&state.passesLeft<=0)return {kind:'ignored'};
  if(action.type==='letter') {
    if(!Number.isInteger(action.slot)||action.slot<0||action.slot>=state.rack.length||state.rackUsed[action.slot])return {kind:'ignored'};
    const words=findWords(state.board,index,state.rack[action.slot],dictionary,state.occupied);
    if(words.length){
      const trial=[...state.board],used=[...state.rackUsed];trial[index]=state.rack[action.slot];used[action.slot]='used';
      const options=allSolutions(trial,dictionary,state.occupied);
      if(!assignRack(trial,state.rack,used,dictionary,options))return {kind:'reserved',index,slot:action.slot,letter:state.rack[action.slot],words:orderedWords(words)};
      return fill(state,index,action.slot,words,false,dictionary);
    }
  }
  if(action.type==='timeout'){
    const assignment=assignRack(state.board,state.rack,state.rackUsed,dictionary,state.options,state.answers),slot=assignment?.get(index);
    if(slot===undefined)throw new Error('The remaining rack does not contain a complete solution.');
    return fill(state,index,slot,findWords(state.board,index,state.rack[slot],dictionary,state.occupied),true,dictionary,'timeout');
  }
  const penalised=canAnswer(state,index);
  if(penalised)gap.misses++;
  if(action.type==='pass')state.passesLeft--;
  nextGap(state,index);
  return {kind:action.type==='letter'?'wrong':'pass',index,slot:action.slot,penalised};
}
export function finishRound(state,elapsedMs) {
  if(!state.ended)return null;
  if(!state.bonusAwarded){
    const budget=Object.values(state.gaps).reduce((sum,g)=>sum+g.allowance,0);
    const bonus=speedBonus(elapsedMs,state.manualSolved,budget);
    state.summary={wordPoints:state.roundPoints,bonus,total:state.roundPoints+bonus,elapsedMs,manualSolved:state.manualSolved,automaticSolved:state.automaticSolved,words:state.roundWords};
    state.score+=bonus;state.bonusAwarded=true;
  }
  return state.summary;
}
